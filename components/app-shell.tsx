"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { Beaker, BookOpenText, ClipboardList, PlayCircle, Upload } from "lucide-react";
import { AuthWidget } from "@/components/auth-widget";
import { cn } from "@/lib/utils";
const links = [{ href: "/", label: "Protocols", icon: ClipboardList }, { href: "/import", label: "Import", icon: Upload }, { href: "/run", label: "Active runs", icon: PlayCircle }, { href: "/logs", label: "Reports", icon: BookOpenText }];
export function AppShell({ children }: { children: React.ReactNode }) { const path = usePathname(); return <div className="min-h-screen"><header className="border-b bg-white/90 backdrop-blur"><div className="mx-auto flex h-16 max-w-6xl items-center justify-between gap-3 px-4"><Link href="/" className="flex items-center gap-2.5 font-bold tracking-tight"><span className="grid size-9 place-items-center rounded-xl bg-moss text-white"><Beaker size={19}/></span><span>LabPilot</span></Link><div className="flex items-center gap-2"><nav className="flex gap-1">{links.map(({href,label,icon:Icon}) => <Link key={href} href={href} className={cn("flex items-center gap-2 rounded-lg px-3 py-2 text-sm font-medium", path === href ? "bg-mist text-moss" : "text-slate-500 hover:bg-slate-100")}><Icon size={16}/><span className="hidden sm:inline">{label}</span></Link>)}</nav><AuthWidget /></div></div></header><main className="mx-auto max-w-6xl px-4 py-8">{children}</main><footer className="mx-auto max-w-6xl px-4 pb-8 text-center text-xs text-slate-400">Data is stored in this browser for this exact site address. Login enables optional Supabase protocol sync; local mode still works without an account.</footer></div>; }
